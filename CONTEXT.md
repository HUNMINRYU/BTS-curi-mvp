# CURI 도메인 용어

## 과목 메타데이터

강의계획서에서 추출하고 검수한 과목의 구조화된 특성이다. 관심 주제, 습득 기술, 수업 활동, 평가 방식, 예상 학습량, 연계 진로를 포함한다. 원문 청크 자체나 모델이 생성한 설명은 과목 메타데이터가 아니다.

## 선택지 taxonomy

여러 과목 메타데이터의 표현을 합쳐 만든 버전이 있는 표준 선택지 집합이다. 같은 의미의 표현을 하나의 값으로 정규화하고, 실제 과목과 연결되지 않는 값은 포함하지 않는다.

## 적응형 선택지

학생이 앞 단계에서 고른 값과 현재 제공 가능한 과목을 기준으로 선택지 taxonomy에서 좁힌 후보이다. 모델이 실시간으로 새 값을 생성하는 선택지가 아니다.

## 사용자 직접 입력

표준 선택지에 없는 학생의 관심 주제나 진로 표현이다. 원문을 보존하되 추천 시에는 taxonomy의 값과 별도로 유사도를 계산한다.

## 추천 근거

학생의 선택 또는 직접 입력과 과목 메타데이터가 어떻게 연결되는지 설명하는 검증 가능한 대응 관계이다. 자연어 추천 문장은 이 대응 관계를 표현할 뿐 근거를 새로 만들지 않는다.

## 교양 탐색

학과 일치 점수를 적용하지 않고 관심 주제, 활동 방식, 학습량, 비선호 조건으로 교양 과목을 찾는 탐색 모드이다.
